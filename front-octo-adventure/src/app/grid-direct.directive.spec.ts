import { GridDirectDirective } from './grid-direct.directive';

describe('GridDirectDirective', () => {
  it('should create an instance', () => {
    const directive = new GridDirectDirective();
    expect(directive).toBeTruthy();
  });
});
